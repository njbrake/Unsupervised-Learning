# Unsupervised-Learning

First, git clone the repo git@github.com:njbrake/Unsupervised-Learning.git

then, install requirements with python3

```
python3 -m pip install -r requirements.txt
```

To generate everything, run

```
python3 main.py --part 5
```